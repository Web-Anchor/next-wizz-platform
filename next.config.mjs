/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'tailwindui.com',
      'images.unsplash.com',
      'img.clerk.com',
      'invoice-storage.b-cdn.net',
    ],
  },
};

export default nextConfig;
